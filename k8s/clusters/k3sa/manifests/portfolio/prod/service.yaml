apiVersion: v1
kind: Service
metadata:
  name: nordbye-service-prod
  namespace: prod-portfolio 
spec:
  selector:
    app: prod-portfolio
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

