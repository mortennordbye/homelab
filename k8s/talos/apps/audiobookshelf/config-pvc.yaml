apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-pvc
  namespace: audiobookshelf
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: proxmox-local
  resources:
    requests:
      storage: 5Gi
