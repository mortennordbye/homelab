apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: audiobookshelf
  namespace: audiobookshelf
  annotations:
    # Must stay unproxied. Cloudflare's terms exclude serving audio through the
    # CDN outside Enterprise, and this route streams it.
    external-dns.kubernetes.io/cloudflare-proxied: "false"
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: traefik-gateway-public
      namespace: traefik
  hostnames:
    - audiobookshelf.bigd.no
  rules:
    - backendRefs:
        # Route through the KEDA HTTP interceptor so audiobookshelf wakes from zero.
        - group: ""
          kind: Service
          name: keda-add-ons-http-interceptor-proxy
          namespace: keda
          port: 8080
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /
