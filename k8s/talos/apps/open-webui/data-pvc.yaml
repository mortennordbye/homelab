apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-pvc
  namespace: open-webui
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: syno-nfs-csi
  resources:
    requests:
      storage: 5Gi
