apiVersion: v1
kind: Service
metadata:
  name: headroom-demo-service
  namespace: headroom-demo
spec:
  selector:
    app: headroom-demo
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 3001
