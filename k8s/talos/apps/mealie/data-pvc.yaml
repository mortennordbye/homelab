apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-pvc
  namespace: mealie
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: syno-nfs-csi
  resources:
    requests:
      storage: 10Gi
