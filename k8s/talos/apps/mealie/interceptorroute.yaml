apiVersion: http.keda.sh/v1beta1
kind: InterceptorRoute
metadata:
  name: mealie
  namespace: mealie
spec:
  target:
    service: mealie-service
    port: 9000
  rules:
    - hosts:
        - mealie.bigd.no
  scalingMetric:
    concurrency:
      targetValue: 100
