# Nightly restic backup of this namespace's app volumes to the NAS
# (docs/backup-plan.md). Every repository shares one password, volsync-restic-password
# in Bitwarden; without it none of them can be restored.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: plex-config-restic
  namespace: plex-media-stack
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-secretsmanager
    kind: ClusterSecretStore
  target:
    name: plex-config-restic
    creationPolicy: Owner
    template:
      engineVersion: v2
      mergePolicy: Replace
      data:
        RESTIC_REPOSITORY: /mnt/repo/plex-media-stack/plex-config
        RESTIC_PASSWORD: "{{ .password }}"
  data:
    - secretKey: password
      remoteRef:
        key: "9facc77a-55f0-4901-a05c-b4d101256b09"
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: plex-config
  namespace: plex-media-stack
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  sourcePVC: plex-config
  trigger:
    schedule: "10 1 * * *"
  restic:
    repository: plex-config-restic
    copyMethod: Direct
    pruneIntervalDays: 7
    retain:
      daily: 14
      weekly: 8
      monthly: 6
    cacheStorageClassName: syno-nfs-csi
    cacheCapacity: 2Gi
    moverVolumes:
      - mountPath: repo
        volumeSource:
          nfs:
            server: nas.local.bigd.no
            path: /volume1/k8s-backups/volsync
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: tautulli-config-restic
  namespace: plex-media-stack
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-secretsmanager
    kind: ClusterSecretStore
  target:
    name: tautulli-config-restic
    creationPolicy: Owner
    template:
      engineVersion: v2
      mergePolicy: Replace
      data:
        RESTIC_REPOSITORY: /mnt/repo/plex-media-stack/tautulli-config
        RESTIC_PASSWORD: "{{ .password }}"
  data:
    - secretKey: password
      remoteRef:
        key: "9facc77a-55f0-4901-a05c-b4d101256b09"
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: tautulli-config
  namespace: plex-media-stack
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  sourcePVC: tautulli-config
  trigger:
    schedule: "10 1 * * *"
  restic:
    repository: tautulli-config-restic
    copyMethod: Direct
    pruneIntervalDays: 7
    retain:
      daily: 14
      weekly: 8
      monthly: 6
    cacheStorageClassName: syno-nfs-csi
    cacheCapacity: 2Gi
    moverVolumes:
      - mountPath: repo
        volumeSource:
          nfs:
            server: nas.local.bigd.no
            path: /volume1/k8s-backups/volsync
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: seerr-config-restic
  namespace: plex-media-stack
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-secretsmanager
    kind: ClusterSecretStore
  target:
    name: seerr-config-restic
    creationPolicy: Owner
    template:
      engineVersion: v2
      mergePolicy: Replace
      data:
        RESTIC_REPOSITORY: /mnt/repo/plex-media-stack/seerr-config
        RESTIC_PASSWORD: "{{ .password }}"
  data:
    - secretKey: password
      remoteRef:
        key: "9facc77a-55f0-4901-a05c-b4d101256b09"
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: seerr-config
  namespace: plex-media-stack
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  sourcePVC: seerr-config
  trigger:
    schedule: "10 1 * * *"
  restic:
    repository: seerr-config-restic
    copyMethod: Direct
    pruneIntervalDays: 7
    retain:
      daily: 14
      weekly: 8
      monthly: 6
    cacheStorageClassName: syno-nfs-csi
    cacheCapacity: 2Gi
    moverVolumes:
      - mountPath: repo
        volumeSource:
          nfs:
            server: nas.local.bigd.no
            path: /volume1/k8s-backups/volsync
